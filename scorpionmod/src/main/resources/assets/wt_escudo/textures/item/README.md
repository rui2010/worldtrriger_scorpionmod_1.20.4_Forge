Place your item PNGs here.

- escudo_item.png : 16x16 or 32x32 recommended

After adding the PNG, rebuild the mod with:

```bash
./gradlew clean build
```

The PNG will be included in the built JAR under assets/wt_escudo/textures/item/
